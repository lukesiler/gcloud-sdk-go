data:
  image-name: lukesiler/gcloud-sdk-plus
  # 0.1.0 - first release with gcloud, go, yq and jq
  # 0.2.0 - second release with ginkgo added
  # 0.2.1 - rebuild of 0.2.0 with version tagging
  # 0.2.2 - rebuild to get ginkgo 1.8.0 with -mod support
  # 0.3.0 - add gcloud datastore emulator
  semver: 0.3.0
